$().ready(() => {}); let sidebarIsVisible = !1; const toggleSidebar = function (e) { const a = !(arguments.length > 0 && void 0 !== e) || e; $("#sidebarNav").toggleClass("sticky", a), $("#stickyNavbarOverlay").toggleClass("active", a), $("#hamburger").toggleClass("is-active"), sidebarIsVisible = a; }; $().ready(() => { $("#hamburger").click(() => { toggleSidebar(!sidebarIsVisible); }), $("#stickyNavbarOverlay").click(() => { sidebarIsVisible && toggleSidebar(!1); }); }); const OFFSET = 150; $().ready(() => {
    const o = $("#side-nav"); const c = []; if ($(".vertical-section").length || o.hide(), $(".vertical-section").each((e, a) => {
        const i = $(a); const t = i.find("> h1").text(); if (t) {
            o.append($("<h3/>").text(t)); const s = $("<ul></ul>"); i.find(".members h4.name").each((e, a) => {
                const i = $(a); const t = i.find(".code-name").clone().children().remove()
                    .end()
                    .text(); const n = i.find("a").attr("href"); const r = $("<a href=\"".concat(n, "\" />")).text(t); s.append($("<li></li>").append(r)), c.push({ link: r, offset: i.offset().top });
            }), o.append(s);
        } else {
            i.find(".members h4.name").each((e, a) => {
                const i = $(a); const t = i.find(".code-name").clone().children().remove()
                    .end()
                    .text(); const n = i.find("a").attr("href"); const r = $("<a href=\"".concat(n, "\" />")).text(t); o.append(r), c.push({ link: r, offset: i.offset().top });
            });
        }
    }), !$.trim(o.text())) return o.hide(); function e() { for (let e = n.scrollTop(), a = !1, i = c.length - 1; i >= 0; i--) { const t = c[i]; t.link.removeClass("is-active"), e + OFFSET >= t.offset ? a ? t.link.addClass("is-past") : (t.link.addClass("is-active"), a = !0) : t.link.removeClass("is-past"); } } var n = $("#main-content-wrapper"); n.on("scroll", e), e(), c.forEach((e) => { e.link.click(() => { n.animate({ scrollTop: e.offset - OFFSET + 1 }, 500); }); });
}), $().ready(() => { $("#sidebarNav a").each((e, a) => { const i = $(a).attr("href"); window.location.pathname.match(`/${i}`) && ($(a).addClass("active"), $("#sidebarNav").scrollTop($(a).offset().top - 150)); }); });
